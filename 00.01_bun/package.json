{
  "name": "00.01_bun",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot index.ts",
    "build": "bun build index.ts --outdir ./dist --target bun",
    "build:single-file": "bun build index.ts --compile --outfile ./dist/app"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
